// BaselineService computes workspace, channel, content-format and objective baselines.
// Performance is compared primarily against the workspace's own historic results.
export class BaselineService {}
